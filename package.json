{
  "name": "milo",
  "version": "0.1.0",
  "private": true,
  "description": "MILO — a local learning assistant. Capture what you learn in the browser; a local agent turns it into a clear picture of what you know.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
